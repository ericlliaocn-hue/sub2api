pid __TEST_ROOT__/nginx.pid;
error_log stderr;

events {
    worker_connections 32;
}

http {
    include __MIME_TYPES__;
    default_type application/octet-stream;
    log_format json_analytics '$status';
    include __TEST_ROOT__/conf.d/*.conf;
}
